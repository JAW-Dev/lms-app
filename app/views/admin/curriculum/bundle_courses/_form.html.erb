<ul class="pl-8">
  <% @courses.each do |course| %>
    <li class="mb-4">
      <%= course.title %> - <%= @bundle.courses.include?(course) ? "YES" : "NO" %>
      <ul class="pl-8">

      </ul>
    </li>
  <% end %>
</ul>
